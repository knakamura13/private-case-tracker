---
auto_execution_mode: 3
description: Commit all changes, then push
---

1. Commit all staged and unstaged changes to the current branch, even if the current branch is main/master.

2. Push the committed changes to the remote repository, creating the remote branch if it doesn't exist.
